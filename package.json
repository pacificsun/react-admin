{
  "name": "react-admin",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "json-server --watch db.json --port 500"
  },
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
